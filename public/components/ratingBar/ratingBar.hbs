<div class="rating">
    <div class="rating-value">{{ratingValue}}</div>
    <div class="rating-stars">
        {{#each items}}
            <div class="star"> {{{this}}} </div>
        {{/each}}
    </div>
</div>
