<div>
    {{#each questions}}
        <h4 id="{{this.num}}">{{this.title}}</h4>
        <ul class="chart">
            {{#each this.options}}
                <li class="chart__bar">{{this.option}}</li>
            {{/each}}
        </ul>
    {{/each}}
</div>
